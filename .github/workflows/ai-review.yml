name: Gemini AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Gemini API로 리뷰 받고 댓글 달기
        uses: actions/github-script@v7
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        with:
          script: |
            const { data: diff_text } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              headers: { accept: 'application/vnd.github.v3.diff' }
            });

            const apiKey = process.env.GEMINI_API_KEY;
            const prompt = `당신은 시니어 소프트웨어 개발자입니다. 다음은 깃허브 PR의 코드 변경사항(diff)입니다. 이를 분석해서 버그, 보안 취약점, 가독성 개선 등에 대한 코드 리뷰를 한국어로 작성해주세요.\n\n${diff_text}`;
            
            // 💡 이 부분이 gemini-2.5-flash 로 수정되었습니다!
            const gemini_url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}`;
            
            const gemini_response = await fetch(gemini_url, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }] })
            });
            
            const result = await gemini_response.json();
            
            if (!result.candidates) {
              console.log(result);
              throw new Error("Gemini API 응답 에러: " + JSON.stringify(result));
            }

            const review_comment = result.candidates[0].content.parts[0].text;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "🤖 **Gemini AI 리뷰 결과:**\n\n" + review_comment
            });
